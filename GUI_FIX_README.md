# BB Tube Compressor — GUI FIX revision

This revision specifically addresses the mismatch visible in the supplied Logic Pro screenshot.
The previous GUI had generic control placement; this revision uses a fixed, integrated hardware layout and attaches each visible control to the matching APVTS parameter.

Build from the repository root using the platform scripts in `MAC_OS/` and `WINDOWS/`.
